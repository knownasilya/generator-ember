{
  "name": "<%= _.slugify(appname) %>",
  "version": "0.0.0",
  "dependencies": {<% if (bootstrap) { %>
    "bootstrap": "~3.0.0",<% } if (bootstrapSass) { %>
    "bootstrap-sass": "~3.0.0",<% } else if (bootstrapStylus) { %>
    "bootstrap-stylus": "~3.0.0",<% } %>
    "ember": "1.0.0",
    "ember-data-shim": "1.0.0-beta.2"
  },
  "resolutions": {
    "ember": "1.0.0"
  },
  "devDependencies": {<% if (testFramework === 'mocha') { %>
    "ember-mocha-adapter": "0.1.2"<% } %>
  }
}
